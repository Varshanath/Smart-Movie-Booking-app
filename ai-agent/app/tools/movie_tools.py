# Placeholder module for the movie recommendation agent's tools.
#
# Intentionally empty for the initial wiring step (Flutter -> Node -> this
# service -> agent -> Node -> Flutter). Tools that call back into the
# existing Node backend (movies/theatres/shows/users APIs) get added here
# in the next step, once end-to-end wiring is confirmed working.
